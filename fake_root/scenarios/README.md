## How to add scenarios

Scenarios must be within a folder containing a main.tscn file

In runtime all paths in the .tscn file should be resolved using local paths which (I believe) must be manually edited when packaging them
